import "../styles/Tips.css"

const TIPS_SEPARATOR = "    ／    ";

const tips: string[] = [
    "石を6つ直線に並べたら勝ち。縦でも横でも斜めでもいい",
    "最初の1手だけが1つ。そのあとはお互い2つずつ置いていく",
    "2つ置けるということは、片方を止められても、もう片方で攻められるということ",
    "4つ並べても、両端を塞がれれば6にはならない。伸ばす先を残しておく",
    "止めるか、作るか。相手の列を止めている間に、自分の列は進んでいない",
    "持ち時間は自分の手番でしか減らない。相手が考えている間に読んでおくと得をする",
    "石を置くたびに持ち時間は増える。迷い続けるより、置いてから考える方が安いこともある",
    "盤の端に寄るほど、伸ばせる方向は減っていく",
];

export function Tips() {
    return (
        <div className="tips-bar">
            <div className="tips-text">{tips.join(TIPS_SEPARATOR)}</div>
        </div>
    );
}
